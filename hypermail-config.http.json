{
  "$schema": "https://raw.githubusercontent.com/hypersimple/hypermail-mcp/main/schemas/hypermail-config.schema.json",
  "dataDir": "~/.hypermail-mcp-dev",
  "http": {
    "enabled": true,
    "port": 3000,
    "host": "127.0.0.1"
  },
  "watch": {
    "enabled": true,
    "pollIntervalSeconds": 10
  },
  "providers": {
    "outlook": {
      "clientId": "${MS_CLIENT_ID}",
      "tenantId": "${MS_TENANT_ID}"
    }
  }
}
